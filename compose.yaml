services:
  starpu:
    build: .
    # stdin_open: true   # Keep the container's stdin open
    # tty: true
    command: bash
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes:
      - ./src:/home/src